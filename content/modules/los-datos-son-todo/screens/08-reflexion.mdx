import { ReflectionPrompt } from "@/components/interactives/reflection-prompt";

Hoy envenenaste un modelo, arreglaste sensores, cazaste un cero de más,
descubriste el sesgo invisible y limpiaste tu primer dataset de verdad.
Ya sabes el secreto del oficio: **los datos son todo**.

<ReflectionPrompt
  moduleSlug="los-datos-son-todo"
  question="De los cuatro tipos de basura que viste (dato roto, atípico, muestra sesgada, hueco), ¿cuál crees que hace más daño y por qué?"
/>

En el próximo módulo cierras el círculo: vas a **entrenar tu propio
modelo** con datos que tú mismo etiquetas — y a verlo fallar exactamente
donde tus etiquetas fallaron.
