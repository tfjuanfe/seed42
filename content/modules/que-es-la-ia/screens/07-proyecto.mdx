import { ProjectClassify } from "@/components/interactives/project-classify";
import { projectItems, iaCategories } from "../data";

Tu primer proyecto: una auditoría. Diez sistemas, y tú decides cuáles
aprendieron de datos y cuáles solo siguen reglas — sin pistas y sin
verificación, como en la vida real.

Al final defiendes tu criterio por escrito. Eso —clasificar y argumentar—
es exactamente lo que hace un auditor de IA profesional.

<ProjectClassify
  moduleSlug="que-es-la-ia"
  items={projectItems}
  categories={iaCategories}
  reasoningPrompt="¿Qué criterio usaste para separar IA de no IA? Escríbelo como se lo explicarías a alguien de tu curso que faltó hoy."
/>
