import { LabelingTask } from "@/components/interactives/labeling-task";
import { breakItems, iaCategories } from "../data";

Tu criterio funcionaba bien. Ahora vamos a romperlo: estos tres casos no tienen respuesta limpia. Decide igual.

<LabelingTask items={breakItems} categories={iaCategories} />

La frontera no está en el aparato: está en **si el comportamiento fue aprendido de datos o escrito a mano**. Y desde afuera, a veces no se puede saber.
