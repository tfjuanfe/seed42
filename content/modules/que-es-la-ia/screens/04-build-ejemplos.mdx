import { LabelingTask } from "@/components/interactives/labeling-task";
import { patternItems, patternCategories } from "../data";

Nadie te va a dar la regla esta vez. Mira los mensajes y decide: ¿spam o normal?

<LabelingTask items={patternItems} categories={patternCategories} />

Detectaste el patrón solo con ejemplos. Un modelo hace lo mismo, pero con millones.
